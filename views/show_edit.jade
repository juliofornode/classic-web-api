extends layout

block content
    h2 #{title}
    form(method='post')
        p
            label(for="name") Name:
            input(type='text', name='name', value=item.name)
        p
            input(type='submit', value='save')